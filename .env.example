# RefGuard 环境变量模板。需要本地覆盖配置时再复制为 .env。
ENVIRONMENT=development
LOG_LEVEL=INFO

# 可选：公开元数据源访问密钥。留空则使用匿名访问。
SEMANTIC_SCHOLAR_API_KEY=
OPENALEX_API_KEY=
CROSSREF_MAILTO=

# 缓存
CACHE_ENABLED=true
CACHE_TTL=3600
CACHE_MAX_SIZE=1000

# 默认不启用遥测。
REFGUARD_TELEMETRY=false
